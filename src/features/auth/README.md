# features/auth/

```
features/auth/
├── commands.ts // server-side orchestration (writes + side effects)
├── service.ts  // core auth logic (tokens, sessions, DB)
├── mail.ts     // email delivery (magic links)
```
